import { Window } from 'happy-dom'
import { beforeEach, it, describe } from 'vitest'
import { test } from './utils'

let window: Window
beforeEach(() => { window = new Window() })

describe('config', () => {
    it.todo('applies a default config')
    it.todo('applies a custom config')
    it.todo('actually uses a config')
})