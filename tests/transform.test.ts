//TODO: Use a screenshot to test the desired image.
import { Window } from 'happy-dom'
import { beforeEach, describe, it } from 'vitest'
import { test } from './utils'

let window: Window
beforeEach(() => { window = new Window() })

describe('resize', () => {
    it.todo('applies the transform')
    it.todo('applies the transform with multiple values')
    it.todo('applies the transform with mixed values')
})
describe('format', () => {
    it.todo('applies the transform')
    it.todo('applies the transform with multiple values')
})
describe('blur', () => {
    it.todo('applies the transform')
    it.todo('applies the transform with multiple values')

})
describe('combined', () => {
    it.todo('applies multiple transforms')
})